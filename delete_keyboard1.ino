void delete_keyboard1() {               // Deletes keyboard 1
  for (int i = 1; i < 1; i++)
  {
    gfx.DeleteButton(i, BLACK);
  }
  gfx.DeleteButton(15, BLACK);
  gfx.DeleteButton(17, BLACK);
}
